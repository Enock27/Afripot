[build]
  command = "SKIP_COMPONENT_TAGGER=true npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

[functions]
  node_bundler = "esbuild"
  directory = "dist/server"
